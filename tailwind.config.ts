// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        jusGreen: "#1B4D3E",
        jusWhite: "#FFFFFF",
        jusBlack: "#000000",
      },
      fontFamily: {
        serif: ['var(--font-playfair)', 'serif'], // High-end restaurant font
        sans: ['var(--font-inter)', 'sans-serif'],
      },
    },
  },
};